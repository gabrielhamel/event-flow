# DDD Lab

## Build event storming UI

```bash
docker build -t event-storming-ui:0.1.0 -f event-storming/ui/Dockerfile .
```

## Build event storming API

```bash
docker build -t event-storming-api:0.1.0 -f event-storming/api/Dockerfile .
```
