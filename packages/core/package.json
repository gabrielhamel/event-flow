{
  "name": "@repo/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./*": {
      "default": "./src/*.ts"
    },
    "./infra/prisma": {
      "default": "./src/infra/prisma/index.ts"
    }
  },
  "scripts": {
    "db:generate": "vp exec prisma generate",
    "db:migrate": "vp exec prisma migrate deploy",
    "db:auth:generate": "vp exec auth generate --adapter prisma --dialect postgresql --config src/infra/auth.ts --output src/infra/prisma/schema.prisma"
  },
  "dependencies": {
    "@hocuspocus/provider": "catalog:",
    "@orpc/contract": "catalog:",
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "better-auth": "catalog:",
    "neverthrow": "^8.2.0",
    "pg": "^8.20.0",
    "uuid": "^13.0.0",
    "yjs": "^13.6.30",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/pg": "^8.20.0",
    "auth": "catalog:",
    "prisma": "catalog:"
  }
}
